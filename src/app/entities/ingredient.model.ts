export class Ingredient {
  //   public name: string;
  //   public amount: number;

  constructor(public name: string, public amount: number) {
    // this.name = name;
    // this.amount = amount;
  }
}

// get rid fo attribute definition and using simple the accessor public in the constructor definition
